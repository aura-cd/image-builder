version: '3'
services:
  web:
    build: ./
    environment:
      - GIT_REPO=
      - DOCKER_BASE_DIR=
      - DOCKER_FILE_PATH=
      - IMAGE_NAME=
      - IMAGE_TAG=
      - DOCKER_REGISTRY=ghcr.io
      - DOCKER_REGISTRY_USER=xpadev-net
      - DOCKER_REGISTRY_PASSWORD=
